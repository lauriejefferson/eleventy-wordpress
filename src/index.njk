---
layout: default.njk
pagination:
  data: posts
  alias: posts
  size: 3
---

<section class="hero">
  <div class="container flex">
    <div class="hero__left">
      <h1 class="hero__title">Hi! I'm Laurie</h1>
      <p class="hero__text">
        Welcome to my digital collection. Here is where I share some random
        thoughts and a list of links to cool websites. I'll also use this site
        to learn more about Headless Wordpress and Eleventy.
      </p>
    </div>
    <div class="hero__images">
      <img
        src="./img/blake-alexander-BYDFN2KkGAQ-unsplash.jpg"
        alt="seashells"
        width="420"
        height="420"
        class="hero__image--one"
      />
      <img
        src="./img/thomas-martinsen-CrnALaUMSA4-unsplash.jpg"
        alt="travelgear"
        width="420"
        height="420"
        class="hero__image--two"
      />
      <img
        src="./img/moujahed-jouini-OEh9R2Q62Xs-unsplash.jpg"
        alt="bags"
        width="420"
        height="420"
        class="hero__image--three"
      />
    </div>
  </div>
</section>
<section class="posts container">
  <h1 class="title">Latest Posts</h1>
  <div class="flex">
    {%- for post in posts %}
    <article class="post">
      <h2 class="post__link">
        <a href="{{ post.slug }}" class="accent">{{ post.title.rendered }}</a>
      </h2>
      <p>{{ post.acf.summary }}</p>

      <small>{{ post.date | postDate }}</small>
      <div class="post__tags">
        {% for tag in post.tags %} {% for tagName in tags %} {% if tag ==
        tagName.id %}
        <small class="tag">{{ tagName.name }}</small>
        {%endif%} {% endfor %} {% endfor %}
      </div>
    </article>
    {% endfor -%}
  </div>
</section>
<section class="links container">
  <h1 class="title">Recent Links</h1>
  <div class="flex">
    {% for link in links %}
    <div class="links">
      <h2 class="links__item">
        <a href="{{ link.url | url }}" class="accent">{{ link.title }}</a>
      </h2>
      <p class="links__text">{{ link.description }}</p>
    </div>

    {% endfor %}
  </div>
  <a href="/links" class="accent">Get All Links</a>
</section>
{#
<section>
  {%- for post in collections.recentPosts %}
  {{ post.content.rendered | safe }}
  {% endfor -%}
</section>
#}
